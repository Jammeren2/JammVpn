# ─── Планы, ТЗ, внутренние заметки (НЕ коммитим) ───────────────
/planning/
/docs/internal/
*.draft.md
NOTES.md
NOTES.local.md
TODO.local.md

# Снапшот фронта для передачи дизайнеру (копия app/ui).
/для дизайнера/

# ─── Rust ──────────────────────────────────────────────────────
/target/
**/target/
*.pdb

# ─── Node / фронтенд / Tauri ───────────────────────────────────
node_modules/
dist/
dist-ssr/
.vite/
*.local
/src-tauri/target/
/src-tauri/gen/

# ─── Сборки / артефакты ────────────────────────────────────────
*.exe
*.msi
*.msix
*.zip
/release/
/out/
# Трассировки профилирования CPU (ETW) — бывают на гигабайты.
*.etl
# Артефакты сборки kernel-драйвера (cl/link).
/wfp-driver/build/
*.sys
*.obj
# Но вшиваемые в exe драйверы (WinpkFilter ndisrd, WinDivert) — храним в репо.
!/crates/platform-windows/drivers/ndisrd/amd64/ndisrd.sys
!/crates/platform-windows/drivers/windivert/WinDivert64.sys

# ─── Секреты и пользовательские конфиги ────────────────────────
.env
.env.*
*.key
*.pem
*.p12
*.pfx
*.cer
config.local.*
*.session

# ─── IDE / редакторы ───────────────────────────────────────────
.vscode/
.idea/
*.user
*.suo
*.sln.docstates

# ─── ОС ────────────────────────────────────────────────────────
Thumbs.db
Desktop.ini
.DS_Store